# Makefile Guide for FastAPI

A complete beginner-friendly guide to using `Makefile` in FastAPI projects.

---

# What is Make?

`make` is a command-line automation tool used to run repetitive development tasks with short commands.

Instead of typing long commands repeatedly:

```bash
venv\Scripts\python -m uvicorn app.main:app --reload